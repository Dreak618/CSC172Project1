
public class BinaryConverter{

    public BinaryConverter(){ // Constructor needed?

    }
    public static long toBinary(String s){ // given input string
        return 0; // for now
    }
    public static String toPlainText(long l){ // given binary
        return ""; // for now
    }
}